spring:
  application:
    name: mypay.mypaycore

server:
   address: <INSERIRE INDIRIZZO IP>

encrypt:
  keyStore:
    alias: aliaskey
    location: file:certs/mypay.mypaycore.jks
    password: <PASSWORD DEL KEYSTORE, COME IN CREAZIONE DEL KEYSTORE>
    secret: <PASSWORD DELLA CHIAVE, COME IN CREAZIONE DEL KEYSTORE>

logging:
  file:
    path: logs
