spring:
  application:
    name: mypay.mypaycore

server:
   address: 127.0.0.1

encrypt:
  keyStore:
    alias: aliaskey
    location: file:certs/keystore.jks
    password: storepass
    secret: keypass

logging:
  file:
    path: logs
